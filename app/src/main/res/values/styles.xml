<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textSize">@dimen/fontSizeNormal</item>
        <item name="android:fontFamily">@font/roboto_light</item>

    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/gray</item>
    </style>

    <style name="backdrop_button" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:layout_marginTop">5dp</item>
        <item name="cornerRadius">16dp</item>
    </style>

    <style name="auth_entry" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="hintTextColor">@color/colorTextSecondary</item>
        <item name="boxStrokeWidth">0dp</item>
        <item name="boxStrokeColor">@color/colorPrimaryLight</item>
        <item name="boxBackgroundColor">@color/colorPrimaryLight2</item>
    </style>

</resources>
